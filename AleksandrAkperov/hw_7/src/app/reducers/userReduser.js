export default function reducer (state, action) {

    switch (action.type) {
        case "FETCH_USER": {
            return {...state, name:action.payload }
            break;
        }
        case "FETCH_USER": {
            return {...state, name:action.payload }
            break;
        }
        case "FETCH_USER": {
            return {...state, name:action.payload }
            break;
        }
    
       
    }
}